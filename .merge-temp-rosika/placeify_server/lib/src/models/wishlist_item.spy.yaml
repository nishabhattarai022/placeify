### Saved product in a customer's wishlist.
class: WishlistItem
table: wishlist_item
fields:
  user: User?, relation(onDelete=Cascade)
  product: Product?, relation(onDelete=Cascade)
  createdAt: DateTime, default=now

indexes:
  wishlist_item_user_product:
    fields: userId, productId
    unique: true
