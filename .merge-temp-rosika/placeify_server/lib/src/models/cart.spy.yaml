### Shopping cart for a user.
class: Cart
table: cart
fields:
  user: User?, relation(onDelete=Cascade)
  createdAt: DateTime, default=now

indexes:
  cart_user_id:
    fields: userId
    unique: true
