abstract final class VendorRoutes {
  static const prefix = '/vendor';
  static const dashboard = '/vendor';
  static const profileFallback = '/profile';
  // Phase 2+: register, orders, products, etc.
}
