import 'local_image_data.dart';

Future<LocalImageData?> readLocalImagePath(
  String source, {
  String? fileName,
}) {
  throw UnsupportedError('readLocalImagePath is not available on this platform.');
}
