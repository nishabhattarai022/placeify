### Vendor-recorded payment status change for a customer order.
class: UserOrderPaymentEvent
fields:
  status: OrderPaymentStatus
  note: String?
  createdAt: DateTime
  ### Raw history key (OrderPaymentStatus or PaymentTransactionStatus name).
  eventKey: String?
  ### Human-readable timeline label derived from existing history.
  displayLabel: String?
