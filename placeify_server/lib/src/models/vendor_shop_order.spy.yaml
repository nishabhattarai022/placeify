### Customer order containing only items from the logged-in vendor's shop.
class: VendorShopOrder
fields:
  orderId: int
  orderNumber: String
  status: OrderStatus
  placedAt: DateTime
  customerName: String
  shippingAddress: String
  vendorTotal: double
  itemCount: int
  items: List<VendorOrderLineItem>
  rejectionReason: String?
  orderPaymentStatus: OrderPaymentStatus
  currentDeliveryStage: DeliveryStage?
