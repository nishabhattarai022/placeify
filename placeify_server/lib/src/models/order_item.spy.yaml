### Line item within an order.
class: OrderItem
table: order_item
fields:
  order: Order?, relation(onDelete=Cascade)
  product: Product?, relation(onDelete=SetNull)
  vendor: Vendor?, relation(onDelete=SetNull)
  quantity: int
  unitPrice: double

indexes:
  order_item_order_id:
    fields: orderId
